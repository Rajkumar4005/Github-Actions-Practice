name: System Info

on: workflow_dispatch

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Print hostname
        run: hostname

      - name: Kernel info
        run: uname -r

      - name: Show OS info

        run: cat /etc/os-release
