name: Usage of pipe

on:
  workflow_dispatch:

jobs:
  pipe:
    runs-on: ubuntu-latest

    steps:
      - name: Multiple echos
        run: |
          echo "Hi"
          echo "123456"
          ls -lrth
          date
          pwd
